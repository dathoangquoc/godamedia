---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import "../styles/blog.css";

import { formatDate } from "../js/script";
import { Image } from "astro:assets";

const { frontmatter } = Astro.props;
const { title, description, date, cover, coverAlt } = frontmatter;
---

<!doctype html>
<html lang="en">
    <head>
        <BaseHead title={title} description={description} />
    </head>
    <body>
        <Header />
        <main class="py-20 w-full">
            <article class="container mx-auto">
                <div class="row justify-end">
                    <div class="w-ful">
                        <!-- frontmatter -->
                        <div class="about_content mx-4 pt-11 lg:pt-15 lg:pb-8">
                            <div class="section_title text-center pb-4">
                                <h1 class="text-4xl font-bold pb-2">
                                    {title}
                                </h1>
                                <h2 class="text-lg text-black font-normal">
                                    {formatDate(date)}
                                </h2>
                            </div>
                            <div class="w-fit mx-auto lg:w-3/4">
                                <Image
                                    src={cover}
                                    alt={coverAlt}
                                    width="1200"
                                    height="600"
                                    format="webp"
                                />
                            </div>
                        </div>

                        <!-- content -->
                        <div class="px-4 lg:px-25 py-2 content">
                            <slot />
                        </div>
                    </div>
                </div>
                <!-- row -->
            </article>
        </main>
        <Footer />
    </body>
</html>
